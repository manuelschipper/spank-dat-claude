#!/bin/bash
# Start spank in silent event-only mode, appending to the shared events file.
# Requires sudo for accelerometer access.
exec sudo /Users/manuelangelschipper/repos/manuelschipper/spank-lab/spank/spank --stdio --silent >> /tmp/spank-events.jsonl
